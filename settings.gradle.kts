pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}

rootProject.name = "ToasterAtSnackBar"

include(":android")
include(":desktop")
include(":common")
include(":toasterAtSnackBar")
